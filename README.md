# LINE 線上點餐系統 (輕量版)

一個輕量級、基於 Vue.js + Tailwind CSS 的線上點餐系統，專為 LINE LIFF 設計，但也可直接在瀏覽器運行。
支援多店家切換、購物車、客製化選項，並預設整合 Google Apps Script (GAS) 作為後端。

## ✨ 特色

- **輕量級**: 無需編譯 (No Build Steps)，直接開啟 `index.html` 即可運作。
- **多店家支援**: 透過 URL 參數載入不同店家菜單 (由 `store_data.js` 管理)。
- **LINE LIFF 整合**: 自動抓取使用者 LINE 資料 (需設定 LIFF ID)。
- **Demo 模式 (GitHub Pages Ready)**: 若無後端連線，自動切換至演示模式，模擬下單流程。

## 🚀 快速開始

### 1. 線上預覽 (GitHub Pages)

直接點擊 Settings -> Pages 開啟 GitHub Pages，即可預覽。

- **預設店家**: `index.html`
- **王府肉燥飯**: `index.html?store=wangfu`
- **桃樂絲手作便當**: `index.html?store=dorothy`

### 2. 本地執行

Clone 此專案，直接用瀏覽器開啟 `index.html`。

```bash
git clone https://github.com/YourUsername/your-repo.git
# 直接點擊 index.html
```

## 🛠️ 設定說明

### 菜單資料 (`store_data.js`)

所有店家資料、菜單、主題顏色都定義在 `store_data.js` 中。你可以自由新增或修改：

```javascript
const DATABASES = {
    'my-store': {
        name: '我的新店家',
        theme: { primary: '#FF0000' },
        menu: [...]
    }
};
```

### 後端串接 (Google Sheets)

若要啟用真實下單功能：

1.  將 `backend/Code_GoogleAppsScript.gs` 的內容複製到 Google Apps Script 專案。
2.  部署為網路應用程式 (Web App)，權限設為 `Anyone (任何人)`。
3.  將取得的 URL 填入 `index.html` 的 `CONFIG.API_URL`。

## 📱 截圖

*(建議在此處放入 demo 截圖)*

## 📄 授權 (License)

MIT
